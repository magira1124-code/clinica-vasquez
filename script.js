document.addEventListener('DOMContentLoaded', () => {
    console.log("Clínica de los Vásquez - Portal web cargado.");
    
    // Aquí en el futuro se conectará con staff.json para cargar los médicos dinámicamente
    // y con config.json para el carrusel de portadas.
});
